imports:
    IUser: '@shared/types/entities/user.interface'
actionGroups:
    user:
        actions:
            initializeData:
                alias: initData
            initializeDataSuccess:
                alias: initSuccess
                payload:
                    user: IUser
                    receivedAt: number
