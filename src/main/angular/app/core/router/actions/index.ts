import { navigateAction } from './navigate.action';
import { backAction } from './back.action';
import { forwardAction } from './forward.action';

export const navigate = navigateAction;
export const back = backAction;
export const forward = forwardAction;
